/**
 * ДЗ #2 — Шаблон для вашего решения.
 *
 * ЧТО НУЖНО СДЕЛАТЬ:
 *    1. Задача 1. От хардкода — к переменным.
 *       Возьмите карточку книги из ДЗ #1 и перепишите:
 *         - Каждое поле — отдельная переменная (val, не var — ничего не меняем).
 *         - Имена в camelCase: title, author, year, pages, price, copiesInStock.
 *         - Вывод — через строковые шаблоны, не конкатенацией:
 *             println("Название: $title")
 *             println("Цена: $price руб.")
 *         - Сумма копий и общая стоимость экземпляров на складе —
 *           посчитайте через выражение в шаблоне:
 *             "Общая стоимость на складе: ${price * copiesInStock} руб."
 *
 *    2. Задача 2. Чтение книги с консоли.
 *       Добавьте перед выводом карточки диалог с пользователем:
 *         Введите название книги: <ввод>
 *         Введите автора: <ввод>
 *         Введите год издания: <ввод>
 *         Введите количество страниц: <ввод>
 *         Введите цену (руб.): <ввод>
 *         Введите количество экземпляров: <ввод>
 *       После ввода — печатается та же карточка, но уже с введёнными данными.
 *       Требования:
 *         - Используйте readln() (а не Scanner).
 *         - Числовые поля приводите к нужному типу:
 *           readln().toInt() / readln().toDouble().
 *         - Все переменные — val (читаем один раз, потом не меняем).
 *
 *    3. Задача 3. Учёт выдач (простая арифметика).
 *       После карточки добавьте блок "Статистика выдач".
 *       Объявите две переменные var:
 *           var totalLoans = 0       // сколько раз книгу брали
 *           var currentlyOnHand = 0  // сколько сейчас на руках у читателей
 *       Симулируйте 3 операции выдачи и 1 возврат —
 *       изменяя var через ++ и -- (или +=/-=):
 *           Выдача №1: на руках 1, всего выдач 1
 *           Выдача №2: на руках 2, всего выдач 2
 *           Выдача №3: на руках 3, всего выдач 3
 *           Возврат:   на руках 2, всего выдач 3
 *           Свободно на полке: <copiesInStock - currentlyOnHand>
 *       💡 Здесь специально нужен var — это редкий, но честный кейс.
 *          Дальше по курсу будем стараться обходиться без них.
 *
 *    4. ★ Бонус (опционально). Задача 4. Форматирование цены.
 *       Цена выводится как 1290.5, а должно быть 1290.50
 *       (две цифры после запятой).
 *       Используйте "%.2f".format(price) или String.format(...).
 */

package com.prosoft.webinar02.homework

fun main() {
    // TODO: 1. Объявить val-переменные для полей карточки книги
    //       и вывести её через строковые шаблоны.

    // TODO: 2. Перед выводом карточки прочитать значения
    //       полей с консоли через readln().

    // TODO: 3. Добавить блок "Статистика выдач" — var totalLoans и
    //       var currentlyOnHand, 3 выдачи и 1 возврат.

    // TODO: 4. (★ Бонус) Вывести цену в формате с двумя знаками
    //       после запятой через "%.2f".format(price).

    println("Замените на свой код.")
}
