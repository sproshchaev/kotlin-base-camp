
| №  | Имя класса            | Тема вебинара                          | Что демонстрирует                                      |
|----|-----------------------|----------------------------------------|--------------------------------------------------------|
| 1  | `WriteTextDemo`       | Запись текста `writeText()`            | Объект File не создаёт файл; запись создаёт его         |
| 2  | `AppendTextDemo`      | Дозапись `appendText()`                | Перезапись против добавления в конец                    |
| 3  | `ReadTextDemo`        | Чтение `readText()` + `exists()`       | Безопасное чтение всего файла одной строкой             |
| 4  | `ReadLinesDemo`       | Чтение `readLines()`                   | Содержимое как List<String>, перебор по строкам         |
| 5  | `ForEachLineDemo`     | Чтение больших файлов `forEachLine()`  | Построчное чтение без загрузки файла в память           |
| 6  | `PathSeparatorDemo`   | Разделитель пути `File.separator`      | Кросс-платформенная сборка пути                         |
| 7  | `CreateFileDirDemo`   | Создание файлов и каталогов            | `mkdir()`, `resolve()`, `createNewFile()`, isFile/isDir |
| 8  | `WalkTreeDemo`        | Обход иерархии `walkTopDown()`         | Рекурсивный обход дерева каталогов                      |
| 9  | `CopyFileDemo`        | Копирование `copyTo()`                 | Копирование файла и параметр `overwrite`                |
| 10 | `ZipReadDemo`         | Архивы `ZipFile` + `use`               | Открытие ZIP, перебор `entries()`, закрытие через use   |
| 11 | `PathApiDemo`         | Современный API `Path`                 | Те же операции чтения/записи через `Path`               |
| 12 | `BackupDemo`          | Практика: резервная копия              | Сборка изученного: backup перед перезаписью             |
