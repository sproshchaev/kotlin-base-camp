
| №  | Имя класса               | Тема вебинара                                | Что демонстрирует                                          |
|----|--------------------------|----------------------------------------------|------------------------------------------------------------|
| 1  | `StringFormatDemo`       | Форматирование `String.format()`             | Спецификаторы `%s`, `%d`, `%.2f`, ширина и ведущие нули     |
| 2  | `StringSplitDemo`        | Разбиение строки `split()`                   | Деление на `List<String>`, разделитель не входит в результат |
| 3  | `StringBuilderDemo`      | Сборка строк `StringBuilder` / `buildString` | Неизменяемость строк, append/insert/reverse, сборка в цикле |
| 4  | `RegexBasicsDemo`        | Создание `Regex` и `matches()`               | Два способа создания, полное совпадение всей строки         |
| 5  | `RegexCharClassesDemo`   | Классы символов                              | Наборы `[abc]`, диапазоны `[0-9]`, сокращения `\d \w`, `[^..]` |
| 6  | `RegexQuantifiersDemo`   | Кванторы                                     | `+`, `*`, `?`, `{n}` — управление числом повторений         |
| 7  | `RegexFindDemo`          | Поиск `find()` и `findAll()`                 | Первое совпадение (nullable) против всех совпадений         |
| 8  | `RegexSplitReplaceDemo`  | `split()` и `replace()` по regex             | Гибкий разделитель, замена всех совпадений, replace с лямбдой |
| 9  | `RegexGroupsDemo`        | Группы и `MatchResult`                       | `groupValues`, `range`, распаковка `destructured`          |
| 10 | `RegexNamedGroupsDemo`   | Именованные группы                           | Синтаксис `(?<name>...)`, доступ по имени и по индексу      |
| 11 | `EmailValidatorDemo`     | Практика: валидация email                    | Якоря `^` и `$`, проверка строки целиком через `matches()` |
| 12 | `PhoneFinderDemo`        | Практика: поиск телефонов                    | `findAll()` + необязательные элементы `?` для форматов      |
