
| №  | Имя класса                    | Тема вебинара                                  | Что демонстрирует                                  |
|----|-------------------------------|------------------------------------------------|----------------------------------------------------|
| 1  | `LambdaBasicsDemo`            | Лямбда и её тип `(Int) -> Int`                 | Лямбда как значение: хранение, передача, вызов     |
| 2  | `HigherOrderFunctionDemo`     | Функция высшего порядка, предикат              | Передаём поведение; `filter` устроен так же        |
| 3  | `FunctionReferenceDemo`       | Ссылки `::fun`, `Class::m`, `obj::m`           | Ссылки против лямбд, 3 вида ссылок                  |
| 4  | `CurryingFactoryDemo`         | Каррирование, функция возвращает функцию       | Фабрика предикатов с предзаполненным аргументом    |
| 5  | `ApplyConfigDemo`             | `apply` (this, возвращает объект)              | Конфигурация объекта при создании                  |
| 6  | `AlsoSideEffectDemo`          | `also` (it, возвращает объект)                 | Логирование/побочный эффект, swap-трюк             |
| 7  | `LetNullSafetyDemo`           | `let` + `?.let` (it, результат лямбды)         | Безопасная работа с null                           |
| 8  | `RunResultDemo`               | `run` (this, результат лямбды)                 | Вычислить результат; run без объекта               |
| 9  | `WithGroupingDemo`            | `with` (this, не extension)                    | Несколько обращений к одному объекту               |
| 10 | `ScopeReturnDifferenceDemo`   | apply/also vs let/run/with                     | Возврат объекта против возврата результата лямбды  |
| 11 | `LambdaWithReceiverDemo`      | Лямбда с получателем `T.() -> Unit`            | Получатель, мини-DSL, основа `apply`               |
| 12 | `BuilderDslDemo`              | Builder через лямбду с получателем             | Декларативное построение объектов                  |
